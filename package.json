{
  "name": "carto-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Carto monorepo",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "npm --workspace create-carto run build",
    "check": "npm --workspace create-carto run check",
    "test": "npm run check && npm --prefix templates/astro-storefront run check && npm --prefix templates/astro-commerce-cloudflare run check"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@11.9.0",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
